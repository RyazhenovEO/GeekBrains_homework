creator - Egor Ryazhenov

# 1. Поработайте с переменными, создайте несколько, выведите на экран,
# запросите у пользователя несколько чисел и строк и сохраните в переменные, выведите на экран


computer_number_1 = 5
computer_word_1 = 'метрология'
computer_name_1 = 'egor'

print (computer_number_1, computer_word_1, computer_name_1)


user_number_1 = int (input ('введите первое число: '))
user_number_2 = int (input ('введите второе число: '))

user_word_1 = input ('введите любое слово: ')
user_name_2 = input ('введите любое имя: ')

print (user_number_1, user_number_2)
print (user_word_1, user_name_2)


# 2. Пользователь вводит время в секундах.
# Переведите время в часы, минуты и секунды и выведите в формате чч:мм:сс.
# Используйте форматирование строк.

user_second = int (input ('введите время в секундах: '))
user_minute = user_second // 60
user_hour = user_minute // 60

user_second = str (user_second)
user_minute = str (user_minute)
user_hour = str (user_hour)

time = user_hour + ':' + user_minute + ':' + user_minute

print (time)


# 3. Узнайте у пользователя число n. Найдите сумму чисел n + nn + nnn

user_number = input ('введите число от 1 до 9: ')

part_1 = user_number
part_2 = part_1 + user_number
part_3 = part_2 + user_number

sum = int (part_1) + int (part_2) + int (part_3)

print (sum)


# 4. Пользователь вводит целое положительное число.
# Найдите самую большую цифру в числе.
# Для решения используйте цикл while и арифметические операции.

user_number = int (input ('введите целое положительное число: '))
max_number = user_number % 10

while user_number >= 1:
    user_number = user_number // 10
    if user_number % 10 > max_number:
        max_number = user_number % 10
    else:
        print("самая большая цифра в числе: ", max_number)
        break


# 5. Запросите у пользователя значения выручки и издержек фирмы.
# Определите, с каким финансовым результатом работает фирма (прибыль — выручка больше издержек, или убыток — издержки больше выручки).
# Выведите соответствующее сообщение. Если фирма отработала с прибылью, вычислите рентабельность выручки (соотношение прибыли к выручке).
# Далее запросите численность сотрудников фирмы и определите прибыль фирмы в расчете на одного сотрудника.

proceeds = int (input ('введите значение выручки: '))
costs = int (input ('введите значение издержек: '))

if proceeds > costs:
    print ('у фирмы прибыль в размере: ', proceeds - costs, ' у.е.')
    employees = int (input ('введите численность сотрудников в фирме: '))
    print ('прибыль фирмы на одного сотрудника составляет: ', proceeds / employees, ' у.е.')
else:
    print ('у фирмы убыток в размере: ', proceeds - costs, ' у.е.')


# 6. Спортсмен занимается ежедневными пробежками.
# В первый день его результат составил a километров.
# Каждый день спортсмен увеличивал результат на 10 % относительно предыдущего.
# Требуется определить номер дня, на который общий результат спортсмена составить не менее b километров.
# Программа должна принимать значения параметров a и b и выводить одно натуральное число — номер дня.

distance_1 = int(input('введите пройденныое расстояние в первый день: '))
distance_2 = int(input('введите желаемое расстояние: '))

day = 1
result = distance_1
while result < distance_2:
    distance_1 = distance_1 + (0.1 * distance_1)
    day += 1
    result = result + distance_1

print ('вы пробежите желаемое расстояние на ', day, ' день')
